{
  "_comment": "Global NPM packages for Docker installation with version constraints. Strategy: Tilde ranges (~X.Y.Z) allow only patch-level updates, preventing breaking changes from minor or major version updates. This provides stricter version control than Poetry-style ranges.",
  "packages": {
    "vitest": "~2.1.8",
    "@vitest/ui": "~2.1.8",
    "@testing-library/react": "~13.4.0",
    "jsdom": "~25.0.1"
  }
}