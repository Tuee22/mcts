# Docker environment configuration
# Copy this to .env and uncomment the configuration you want

# For CPU build (default):
TARGET=cpu
PLATFORM=linux/amd64
RUNTIME=runc
PORT=8000

# For CUDA build (NVIDIA GPU systems):
# TARGET=cuda
# PLATFORM=linux/amd64
# RUNTIME=nvidia
# PORT=8000

# For ARM build (Apple Silicon, etc):
# TARGET=arm
# PLATFORM=linux/arm64
# RUNTIME=runc
# PORT=8000

# Advanced configuration options:
# TARGET        - Build target: cpu, cuda, or arm
# PLATFORM      - Docker platform: linux/amd64, linux/arm64
# RUNTIME       - Container runtime: runc (default), nvidia (for GPU)
# PORT          - External port mapping (default: 8000)

# Usage examples:
# 1. CPU build:        docker compose up -d
# 2. CUDA build:       TARGET=cuda RUNTIME=nvidia docker compose up -d
# 3. ARM build:        TARGET=arm PLATFORM=linux/arm64 docker compose up -d
# 4. Custom port:      PORT=9000 docker compose up -d

# Notes:
# - CUDA builds require NVIDIA Docker runtime and compatible GPU
# - ARM builds use QEMU emulation and may be slower
# - The nvidia/cuda:12.6.2-devel-ubuntu22.04 base image is used for CUDA builds
# - All builds include nvidia-smi for GPU verification (CUDA builds only)